import { FieldsArrayFormDirective } from './fields-array-form.directive';

describe('FieldsArrayFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldsArrayFormDirective();
    expect(directive).toBeTruthy();
  });
});
